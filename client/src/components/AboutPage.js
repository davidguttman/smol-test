import React from 'react';

const AboutPage = () => {
    return (
        <div>
            <h1>About</h1>
            <p>This is a web application that allows users to enter a URL. The application will then scrape the HTML from the provided URL, store it in a database, and provide a shareable copy of the scraped data.</p>
        </div>
    );
}

export default AboutPage;